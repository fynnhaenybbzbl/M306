spring:
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  datasource:
    url:  jdbc:postgresql://localhost:5432/m306test
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: none
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect